### Init the Repository for configs files

1. Init the repo
   ```sh
   git init
   ```

2. Add all files
   ```sh
   git add .
   ```

3. Commit all files
   ```sh
   git commit -m "Initial commit"
   ```